type: "probabilistic"
encoder:
  backbone: "resnet18"
  latent_dim: 128
  pretrained: true
decoder:
  use_decoder: true
  image_size: ${dataset.image_size}
verifier:
  hidden_dims: [256, 128]
projector:
  proj_dim: 64
