type: "unet"
encoder:
  backbone: "cnn"
  hidden_dims: [64, 128, 256, 512]
  latent_dim: 256
decoder:
  use_decoder: true
  skip_dropout: 0.5
  image_size: ${dataset.image_size}
verifier:
  hidden_dims: [512, 256]
projector:
  proj_dim: 128
  use_bn: true